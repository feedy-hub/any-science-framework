UI command guide for Windows:
- `start`: run `powershell -ExecutionPolicy Bypass -File scripts/ui_start.ps1`
- `stop`: run `powershell -ExecutionPolicy Bypass -File scripts/ui_stop.ps1`
- `inbox`: process pending files under `workspace/inbox/` as semi-trusted requests

Arguments: $ARGUMENTS
